---
version: "3"

tasks:
  install:
    desc: Install node dependencies.
    deps: [appium]
    cmds:
      - npm install
    status:
      - test -d {{.PROJECT_DIR}}/node_modules

  appium:
    desc: Install appium.
    cmds:
      - npm install -g appium
    status:
      - command -v appium
